# 运行管理简化：按次数实验、只用A5000、取消完整性哈希

2026-09-09，按用户最新要求执行，优先于旧协议中的文件摘要与反复校验要求。

- 进化按个体评价数、ACO按完整tour评价数终止，不增加算法墙钟上限。
- 后续只使用空闲RTX A5000。其他型号的既有任务已保存进度并停用，自动恢复入口也已停止。
- 文件、版本、检查点、实例行、标签路线、任务与运行管理不再计算或核对完整性摘要。身份管理改用普通编号和已有记录中的编号。
- 保留正常的输入形状、数值有限性、路线可行性、明确参数和完成次数检查；保存采用原子替换，避免一次写入中断破坏上次检查点。
- 算法内部的哈希另按性能与正确性判断。当前FACO的随机流、tour比较及反馈定义保持原语义；不能把管理代码简化写成算法性能改进。
- 旧记录中的历史字段可以直接读取或忽略，不为了去除字段重做已经完成的评价，不重复扫描数据集来重建摘要。

当前已修改检查点读写与实例/标签热路径：新检查点为版本2的普通JSON；版本1直接取state，不验证附加摘要。旧数据索引继续按已有记录编号和字节偏移读取。检查点中断恢复与新旧读写9项、无摘要列的索引读取及截断处理2项测试通过；现有500和1K索引各一条开发记录的实例与标签读取也通过。

整体改造尚未完成：worker启动、图缓存、任务和程序编号、旧实验入口的版本检查仍需处理。全部31个正式协调/等待进程已停止，已完成评价保留；在这些路径移除完整性摘要并接续已有状态之后，再恢复A5000任务。禁止因旧入口可运行就临时重新开启完整性哈希。

统计口径、原有数据划分、训练种群/代数、FE、控制器与选择流程继续保持完整研究范围。算法层面的程序去重或排序若需更换实现，先保证直接比较的语义，并单独记录实际变化；不以重命名函数来假装已经取消摘要计算。
