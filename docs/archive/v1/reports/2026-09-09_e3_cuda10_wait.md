# cuda10 两个 E3 运行的保留返回与空闲等待

`GP-alpha-Escape-3313` 和 `GP-POPMUSIC-Hard-3313` 分别在第 392 与 437 次求解返回后观察到外来 GPU 进程；原 CLI 句柄 77514、31752 均已取得 exit 1，原 worker PID 660470、660469 均已不存在。原始观察分别为两个与一个外来进程；随后设备上仍有其他计算进程，未干预这些进程。

按先前已登记的统一 [资源恢复政策](2026-09-09_e3_resource_recovery.md)，两个唯一实际返回经原 evaluator 补齐入账，新增原生提交均为 0。原 395 与 440 条完成记录、worker 历史和原始返回/task 字节全部保持。两个完整暂停前缀均通过独立审计，累计 829 调用、108,658,688 FE、26,528 个成员，失败求解 0；各自已完成第一代并处于第二代前缀，不能视为完整 50 代终态。

完整训练现由等待句柄 49075、76972 承接。[等待入口](https://github.com/LinHuanli/GPLSACO/blob/b6b1c57ba8a825b5ab0211bf38a31cafc579be6f/scripts/wait_e3_recovered_run.py)在原 host/UUID 的严格空闲观察前不创建 CUDA 上下文、不提交 evaluation；等待没有墙钟上限。空闲后核对原暂停点、入口和政策摘要，自动继续原 128×50 完整训练。若启动竞争或其他错误导致 CLI 再次退出，保留实际终态供检查，不自动重复原生求解。改变占用的那批调用及受影响整次计时继续单独分类。

源码固定为 `b6b1c57ba8a825b5ab0211bf38a31cafc579be6f`，原恢复入口与政策仍为 a3a395f 版本。等待启动时状态为 `waiting_for_idle_gpu`，仍待实际 worker 启动和新增返回；其他运行保持原句柄。另一个 POPMUSIC-Hard seed5519 仍由原 Static 队列接续，正式 TEST 未放行。

证据见 [e3_cuda10_resource_wait_results.json](../../../../results/v1/e3_cuda10_resource_wait_results.json)。原始 checkpoint/task/raw、完整暂停快照、审计日志及请求保留于 E3 工作树 `artifacts/e3/resource-recovery-v2/`，并归档至 main 的 `artifacts/runtimes/b6b1c57ba8a825b5ab0211bf38a31cafc579be6f/`。证据包 SHA256 为 `54acf9cf00830ce05da45dafdfcc18c58af79eaae56ee277cb04eb85cc0afa8d`。

首次审计调用早于快照复制句柄 68888 结束，因 manifest 尚未复制而 exit 1，日志保留。待原复制进程实际 exit 0 后，审计句柄 11165、43222 均 exit 0。该审计编排错误未触发任何原生重新评价。
