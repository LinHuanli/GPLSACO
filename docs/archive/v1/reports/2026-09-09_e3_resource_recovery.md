# E3 资源争用后的实际返回恢复

`GP-alpha-Hard-5519` 在 cuda00 的第 66 次原生求解已返回，但结束时观察到一个外来 compute process，原 CLI 以 1 退出。原始异常、原返回及此前全部 67 条完成收据已封存。新增资源偏差政策保留实际返回、继续原 evaluator 和失败评分，并把受影响任务及整次运行计时排除于独占 GPU 的速度比较；不重复原生求解，不按结果质量重抽样。

恢复实现先在 E3 隔离分支的提交 `a3a395f09623747c4b5542f3e19578989dc2e5c9` 固定并推送，七项针对性检查通过。实际补齐入账 CLI exit 0，新增原生提交为 0，原完成收据、worker 历史及原返回字节全部保持。66 调用 / 8,650,752 FE 的暂停点和随后 77 调用 / 10,092,544 FE 的完整恢复前缀均通过独立审计；后一快照 2,464 个成员、0 失败，最大独立成本误差 `4.62e-14`。

完整训练已由句柄 91788 在原 host/UUID 继续，worker PID 3016004。原 128×50、每 colony 4,096 FE、图、native、实例、seed、评分和选择规则不变；新入口只在未提交任务前等待空闲，不设置算法时间上限。原执行 manifest 不改，新增编排实现和政策分别记录摘要，不能把恢复全部实现视为原 7fbe5c8。

UTC 19:43 的活动观察为 E3 GP 6,631 调用 / 869,138,432 FE，Static 1,864 调用 / 244,318,208 FE，共 23 个活动分组；一个 GP 仍由固定设备队列接续。主树十个 E1 为 24,082 调用 / 3,156,475,904 FE，基线 2,445 调用 / 320,471,040 FE，均尚无失败求解，尚未完整结束。此为活动观察，不能作为研究终态或 TEST 放行证据。

机器记录见 [e3_resource_recovery_results.json](../../../../results/v1/e3_resource_recovery_results.json)。独立工作树为 `.tmp/worktrees/e3-candidate-export`，完整说明与源码保存在 Git 的 `work/e3-candidate-export` 分支；原 E1 冻结代码继续留在 main。原始材料、源码和两个不可变快照存放于 `artifacts/runtimes/a3a395f09623747c4b5542f3e19578989dc2e5c9/`，恢复后证据包 SHA256 为 `6213bae31b553bc04fb8a1f99cf772c5ca6640214484b9d2a78d4f2c9bb918d2`。完整 E1–E4、训练终态、选择封存与正式 TEST 仍待完成。
