# E1未提交资源暂停的原地接续

2026-09-09。在完整训练和TEST放行之前，统一适用于明确的提交前资源占用。当前Full-2207、Full-5519、NoFeedback-2207原CLI以1退出；最后待办attempts为空、无任务返回文件，提交前日志分别记录1个外来进程。既有已完成任务和FE全部保留。

独立等待器[scripts/wait_e1_resource.py](../../scripts/wait_e1_resource.py)只在原host/UUID无compute process、显存≤1024MiB、util≤5%时启动原冻结`research_e1.py train --resume`。等待不创建CUDA上下文、不提交求解、不消耗FE，不设算法墙钟上限；保持完整128×50、全部五seed、原面板、验证和FE设置。先对静止快照完成原独立审计，再将报告、checkpoint、入口和等待器SHA绑定到请求。

若接续CLI再次以相同“提交前有外来进程”异常退出，且新的待办仍无attempt、无返回文件，等待器可继续等待原GPU。每次CLI的日志、资源和退出码分别保留；每次都核对原run ID及所有既有completed hash未改变。实际返回、running尝试、无资源证据异常、普通求解失败均不走此自动路径，交回单独核查。原任务或入口被其他操作者推进/修改时停止等待器，避免重复运行。

资源观察与质量失败分列；已有求解的真实后置占用记录不改写。受资源干扰运行的整体计时不能与未观察到干扰的运行合并为clean-speed比较。最终仍需完整终态和独立审计；前缀/等待器验收不表示E1完成。
